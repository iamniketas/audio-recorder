# AudioRecorder

**AudioRecorder** — это нативное Windows-приложение для записи и интеллектуальной расшифровки аудио с фокусом на приватность и локальную обработку данных. Программа позволяет захватывать системный звук, микрофон и импортировать файлы, превращая их в структурированные текстовые заметки с помощью ИИ.

## ✨ Основные возможности

* **Захват звука:** Запись системного выхода (звонки, видео, игры) через WASAPI Loopback и микрофона одновременно.
* **Локальный STT:** Расшифровка текста с использованием **Whisper** (Whisper.cpp или Faster-Whisper). Все данные остаются на вашем устройстве.
* **Диаризация:** Автоматическое разделение спикеров (S1, S2...) в записи с поддержкой переименования.
* **Воспроизведение по таймстемпу:** Клик на временную метку воспроизводит соответствующий отрывок аудио.
* **Редактирование транскрипции:** Изменяйте текст сегментов, переименовывайте спикеров. Индикатор изменений отслеживает несохранённые правки.
* **Интеллектуальное резюмирование:** Выделение ключевых мыслей, задач и решений из текста (LLM post-processing).
* **Семантический поиск:** Поиск по смыслу во всей базе записей благодаря векторному индексированию (RAG).
* **Экспорт:** Сохранение результатов в формате Markdown для удобной интеграции в ваши рабочие процессы.

## 🛠 Технологический стек

* **UI:** WinUI 3 + .NET 8 (Fluent Design для Windows 11).
* **Audio:** NAudio + WASAPI.
* **ML Runtime:** ONNX Runtime с поддержкой CUDA для GPU-ускорения.
* **Хранение:** SQLite + sqlite-vec для векторного поиска.

## 🎯 Что нового в v0.2.2

* **🔧 Исправлен краш на длинных файлах:** Решена проблема с ошибкой `-1073740791` при расшифровке файлов длительностью 60+ минут
* **📊 Детальный прогресс транскрипции:** Отображение процента, скорости (audio seconds/s), прошедшего и оставшегося времени в реальном времени
* **📈 Статистика результатов:** Количество символов, слов и размер выходного файла после завершения транскрипции
* **🔍 Логирование Whisper:** Полный лог процесса транскрипции сохраняется рядом с результатом для отладки (`{имя}_whisper.log`)
* **⚡ Оптимизация памяти:** Улучшена обработка вывода процесса Whisper для предотвращения переполнения памяти на больших файлах

## 🚀 Быстрый старт (для разработчиков)

Проект находится в стадии активной разработки.

1. Клонируйте репозиторий.
2. Откройте файл решения `AudioRecorder.sln` в Visual Studio 2022.
3. Соберите проект:
   ```bash
   dotnet build AudioRecorder.sln
   ```
Подробные инструкции по архитектуре и командам разработки доступны в файле CLAUDE.md.

## ⚖️ Отказ от ответственности (Legal Disclaimer)
Данное программное обеспечение предоставляется «как есть» (as is) и предназначено исключительно для законного использования (личные заметки, протоколирование собственных встреч).

* Соблюдение законов: Пользователь несет единоличную ответственность за соблюдение местного законодательства в отношении записи разговоров. В многих юрисдикциях требуется получение согласия всех сторон перед началом записи.
* Приватность: Автор не имеет доступа к вашим записям. Вся обработка (STT) происходит локально. Однако вы сами отвечаете за безопасность экспортированных файлов.
* Отсутствие гарантий: Автор не несет ответственности за любой ущерб или юридические последствия, возникшие в результате использования данного ПО.

## 📄 Лицензия
Проект распространяется под лицензией Apache License 2.0.

Это позволяет вам свободно использовать, модифицировать и распространять код, включая коммерческое использование, обеспечивая при этом защиту от патентных споров. Подробности в файле LICENSE.

## 🗺 Дорожная карта (Roadmap)

### ✅ v0.2.2 (текущая версия)
* Исправлен критический краш при транскрипции длинных файлов (60+ минут)
* Детальный прогресс с отображением скорости, времени и статистики
* Логирование процесса Whisper для диагностики
* Оптимизация обработки больших файлов

### ✅ v0.2.1
* Исправлен парсинг расшифровки для корректной обработки временных меток
* Полнофункциональный UI для редактирования: таймстемпы, имена спикеров, текст
* Воспроизведение аудиосегментов по клику на таймстемп
* Индикатор несохранённых изменений
* Улучшена эстетика интерфейса (компактная раскладка, tooltips, обрезка длинных путей)

### 📋 v0.3 (следующая)
* Семантический поиск по всем записям (RAG с BGE-M3)
* LLM post-processing: автоматическое извлечение задач, решений, ключевых моментов
* Сохранение структурированной информации в SQLite

### 📅 v0.4+
* Автоматические протоколы встреч
* Интеграция с Telegram/Slack для экспорта
* Поддержка дополнительных языков
* Версия для macOS (SwiftUI + Core Audio)

## 📦 Установка для друга (Windows)

Если вы отправляете ссылку на **GitHub Releases**, попросите скачать:

1. Файл вида `AudioRecorder-Setup.exe` (или другой `*-Setup.exe` из релиза).
2. Запустить этот `.exe`.
3. После установки открыть `AudioRecorder` через меню Пуск.

Что **не нужно** скачивать вручную:
- `*.nupkg`
- `RELEASES`
- исходники (`Source code.zip/tar.gz`)

Обновления в приложении:
1. Нажать `Проверить обновления` в блоке "Обновления".
2. Дождаться окончания загрузки.
3. Нажать `Установить обновление` (приложение перезапустится).

Модель Whisper:
1. При первом запуске приложение автоматически начнет загрузку `large-v2` (если модели нет).
2. Прогресс загрузки отображается в секции "Транскрипция".
3. При необходимости можно отменить и запустить загрузку вручную кнопкой `Скачать модель Whisper`.

Расположение в production (по умолчанию):
- `%LocalAppData%\AudioRecorder\runtime\faster-whisper-xxl\`
- EXE: `%LocalAppData%\AudioRecorder\runtime\faster-whisper-xxl\faster-whisper-xxl.exe`
- Модели: `%LocalAppData%\AudioRecorder\runtime\faster-whisper-xxl\_models\`

Экспортируемые переменные окружения (user/process):
- `AUDIORECORDER_WHISPER_EXE`
- `AUDIORECORDER_WHISPER_ROOT`
- `AUDIORECORDER_WHISPER_MODELS_DIR`
- `AUDIORECORDER_WHISPER_MODEL_LARGE_V2_DIR`
