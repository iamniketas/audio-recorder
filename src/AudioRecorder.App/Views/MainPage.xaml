<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="AudioRecorder.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:AudioRecorder"
    xmlns:views="using:AudioRecorder.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Content>
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock
                Grid.Row="0"
                FontSize="28"
                FontWeight="SemiBold"
                Margin="0,0,0,24"
                Text="AudioRecorder" />

            <!-- Основной контент -->
            <StackPanel
                Grid.Row="1"
                MaxWidth="600"
                Spacing="16">

                <!-- Выбор источников аудио (множественный) -->
                <StackPanel Spacing="12">
                    <TextBlock
                        FontSize="16"
                        FontWeight="Medium"
                        Text="Источники аудио (выберите один или несколько)" />

                    <!-- Источники ВЫВОДА (системный звук) -->
                    <StackPanel Spacing="4">
                        <TextBlock
                            FontSize="14"
                            FontWeight="SemiBold"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            Text="🔊 Вывод (системный звук)" />
                        <Border
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            MaxHeight="150">
                            <ListView
                                x:Name="OutputSourcesListView"
                                SelectionMode="None"
                                ItemsSource="{x:Bind OutputSources, Mode=OneWay}">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="views:AudioSourceViewModel">
                                        <CheckBox
                                            Content="{x:Bind Source.Name}"
                                            IsChecked="{x:Bind IsSelected, Mode=TwoWay}"
                                            Checked="OnSourceSelectionChanged"
                                            Unchecked="OnSourceSelectionChanged" />
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                    </StackPanel>

                    <!-- Источники ВВОДА (микрофоны) -->
                    <StackPanel Spacing="4">
                        <TextBlock
                            FontSize="14"
                            FontWeight="SemiBold"
                            Foreground="{ThemeResource AccentTextFillColorSecondaryBrush}"
                            Text="🎤 Ввод (микрофоны)" />
                        <Border
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            MaxHeight="150">
                            <ListView
                                x:Name="InputSourcesListView"
                                SelectionMode="None"
                                ItemsSource="{x:Bind InputSources, Mode=OneWay}">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="views:AudioSourceViewModel">
                                        <CheckBox
                                            Content="{x:Bind Source.Name}"
                                            IsChecked="{x:Bind IsSelected, Mode=TwoWay}"
                                            Checked="OnSourceSelectionChanged"
                                            Unchecked="OnSourceSelectionChanged" />
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <!-- Статус записи -->
                <Border
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock
                            FontSize="16"
                            FontWeight="Medium"
                            Text="Статус" />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="0"
                                Margin="0,0,12,8"
                                Text="Состояние:" />
                            <TextBlock
                                x:Name="StateTextBlock"
                                Grid.Row="0"
                                Grid.Column="1"
                                Margin="0,0,0,8"
                                FontWeight="SemiBold"
                                Text="Остановлено" />

                            <TextBlock
                                Grid.Row="1"
                                Grid.Column="0"
                                Margin="0,0,12,8"
                                Text="Длительность:" />
                            <TextBlock
                                x:Name="DurationTextBlock"
                                Grid.Row="1"
                                Grid.Column="1"
                                Margin="0,0,0,8"
                                Text="00:00:00" />

                            <TextBlock
                                Grid.Row="2"
                                Grid.Column="0"
                                Margin="0,0,12,0"
                                Text="Размер:" />
                            <TextBlock
                                x:Name="FileSizeTextBlock"
                                Grid.Row="2"
                                Grid.Column="1"
                                Text="0 MB" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Кнопки управления -->
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button
                        x:Name="StartStopButton"
                        Grid.Column="0"
                        HorizontalAlignment="Stretch"
                        Click="OnStartStopClicked"
                        Content="Начать запись"
                        IsEnabled="False" />

                    <Button
                        x:Name="PauseResumeButton"
                        Grid.Column="1"
                        HorizontalAlignment="Stretch"
                        Click="OnPauseResumeClicked"
                        Content="Пауза"
                        IsEnabled="False" />
                </Grid>

                <!-- Кнопка импорта файла -->
                <Button
                    x:Name="ImportButton"
                    HorizontalAlignment="Stretch"
                    Click="OnImportClicked"
                    Content="Импортировать аудиофайл" />

            </StackPanel>
        </Grid>
    </Page.Content>
</Page>
